import { createReducer } from "@reduxjs/toolkit";



export const simpleReducer = createReducer(initialState, (builder)=>{
        builder.addCase('navbarData', (state, action)=>{
            
        })
})